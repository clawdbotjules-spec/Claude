{
  "name": "poker-solver-g2",
  "version": "1.0.0",
  "description": "GTO poker solver for Even Realities G2 smart glasses",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "pack": "npm run build && evenhub pack"
  },
  "dependencies": {
    "@evenrealities/even_hub_sdk": "^0.0.9"
  },
  "devDependencies": {
    "@evenrealities/evenhub-cli": "^0.1.9",
    "typescript": "^5.4.0",
    "vite": "^6.0.0"
  }
}
